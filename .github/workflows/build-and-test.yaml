name: llround Bug Test

jobs:
  hello:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Test llround(-2.5)
        run: |
          cat << 'EOF' > test_llround.c
          #include <stdio.h>
          #include <math.h>

          int main() {
              double value = -2.5;
              long long result = llround(value);
              printf("llround(-2.5) = %lld\n", result);
              return 0;
          }
          EOF
          gcc test_llround.c -o test_llround -lm
          ./test_llround
